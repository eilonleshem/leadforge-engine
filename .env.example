# ============================================
# LeadForge Engine - Environment Variables
# ============================================
# Copy this file to .env.local for local development.
# In Vercel, set these via Settings > Environment Variables.
# ============================================

# ---- Database (required) ----
# Use a hosted Postgres provider: Neon, Supabase, Railway, etc.
DATABASE_URL=postgresql://user:password@host:5432/leadforge?sslmode=require

# ---- NextAuth (required) ----
# Generate a strong secret: openssl rand -base64 32
NEXTAUTH_SECRET=
# In production on Vercel this is auto-detected; set explicitly for preview envs
NEXTAUTH_URL=http://localhost:3000

# ---- Admin credentials (required for env-based auth; optional if using DB auth) ----
ADMIN_EMAIL=admin@leadforge.com
ADMIN_PASSWORD=change-me-in-production

# ---- Upstash Redis (required for OTP + rate limiting) ----
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ---- Twilio (required for SMS OTP + call tracking) ----
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+1XXXXXXXXXX
TWILIO_TRACKING_NUMBER=+1XXXXXXXXXX

# ---- App ----
APP_BASE_URL=http://localhost:3000

# ---- Public (exposed to browser - optional) ----
# If you want the tracking number visible on the client-side Call Now button:
# NEXT_PUBLIC_TRACKING_NUMBER=+1XXXXXXXXXX
